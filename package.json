{
  "name": "bboreporters",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:backend": "cd backend && npm run dev",
    "start:frontend": "cd frontend && npm start",
    "build:render": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start:render": "cd backend && npm start",
    "start": "echo Use npm run start:backend or npm run start:frontend"
  }
}
